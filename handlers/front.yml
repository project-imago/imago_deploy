handlers:
  - name: "reload systemd"
    systemd:
      daemon_reload: yes
    listen: "reload systemd"

  - name: "restart front"
    service:
      name: "front"
      state: restarted
      enabled: yes
    listen: "restart front"